<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb Home - Explore</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-800">

    <!-- Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex-shrink-0 flex items-center">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-bold text-2xl tracking-tighter">airbnb</a>
                </div>
                <div class="hidden md:flex space-x-8">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-medium border-b-2 border-red-500 px-3 py-2">Home</a>
                    <a href="/homelist?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Listings</a>
                    <a href="/favourite?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Favorites</a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm font-medium text-gray-500">Hi, <%= username %></span>
                    <div class="h-8 w-8 rounded-full bg-gray-200 flex items-center justify-center text-gray-500">
                        <i class="fas fa-user"></i>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="bg-black text-white py-20 px-6 text-center"
        style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1501785888041-af3ef285b470?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80'); background-size: cover; background-position: center;">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">Find your next adventure</h1>
        <p class="text-lg md:text-xl mb-8 opacity-90">Discover homes, cabins, and unique stays around the world.</p>
        <div class="max-w-3xl mx-auto bg-white rounded-full p-2 flex shadow-lg text-gray-800">
            <div class="flex-grow px-4 py-2 border-r">
                <label class="block text-xs font-bold text-gray-800 uppercase tracking-wider">Location</label>
                <input type="text" placeholder="Where are you going?"
                    class="w-full text-sm outline-none bg-transparent">
            </div>
            <div class="px-4 py-2 border-r hidden sm:block">
                <label class="block text-xs font-bold text-gray-800 uppercase tracking-wider">Check in</label>
                <input type="text" placeholder="Add dates" class="w-full text-sm outline-none bg-transparent">
            </div>
            <button
                class="bg-red-500 hover:bg-red-600 text-white rounded-full h-12 w-12 flex items-center justify-center transition">
                <i class="fas fa-search"></i>
            </button>
        </div>
    </div>

    <!-- Listings Grid -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-8">Explore nearby stays</h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            <% homes.forEach(home=> { %>
                <div
                    class="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-xl transition duration-300 transform hover:-translate-y-1 group">
                    <div class="relative h-64 overflow-hidden">
                        <img src="<%= home.imageUrl %>" alt="<%= home.title %>"
                            class="w-full h-full object-cover transform group-hover:scale-110 transition duration-500">
                        <div class="absolute top-3 right-3">
                            <button class="text-white hover:text-red-500 transition">
                                <i class="far fa-heart text-2xl drop-shadow-md"></i>
                            </button>
                        </div>
                    </div>
                    <div class="p-6">
                        <div class="flex justify-between items-start mb-2">
                            <div>
                                <h3 class="text-xl font-bold text-gray-900">
                                    <%= home.title %>
                                </h3>
                                <p class="text-sm text-gray-500"><i class="fas fa-map-marker-alt mr-1"></i>
                                    <%= home.location %>
                                </p>
                            </div>
                            <div class="flex items-center text-sm font-semibold">
                                <i class="fas fa-star text-yellow-400 mr-1"></i> 4.9
                            </div>
                        </div>
                        <p class="text-gray-600 text-sm mb-4 line-clamp-2">
                            <%= home.description %>
                        </p>
                        <div class="flex items-center justify-between mt-4 border-t pt-4">
                            <p class="text-lg font-bold text-gray-900">$<%= home.price %> <span
                                        class="text-sm font-normal text-gray-500">/ night</span></p>
                            <a href="/homedetails/<%= home.id %>?username=<%= encodeURIComponent(username) %>"
                                class="bg-gradient-to-r from-red-500 to-pink-500 hover:from-red-600 hover:to-pink-600 text-white font-medium py-2 px-6 rounded-lg shadow transition transform hover:scale-105">
                                Book
                            </a>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-100 py-8 border-t mt-12">
        <div class="max-w-7xl mx-auto px-4 text-center text-gray-500 text-sm">
            &copy; 2024 Airbnb Clone. All rights reserved.
        </div>
    </footer>

</body>

</html>