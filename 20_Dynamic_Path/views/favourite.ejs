<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb - Favourites</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-800">

    <!-- Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex-shrink-0 flex items-center">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-bold text-2xl tracking-tighter">airbnb</a>
                </div>
                <div class="hidden md:flex space-x-8">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Home</a>
                    <a href="/homelist?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Listings</a>
                    <a href="/favourite?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-medium border-b-2 border-red-500 px-3 py-2">Favorites</a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm font-medium text-gray-500">Hi, <%= username %></span>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="text-center mb-12">
            <h1 class="text-3xl font-bold text-gray-900 mb-4">Your Favorites</h1>
            <p class="text-gray-600 text-lg">Save these spots for your future adventures.</p>
        </div>

        <div
            class="flex flex-col items-center justify-center py-20 bg-white rounded-xl border border-dashed border-gray-300">
            <div class="text-6xl text-gray-200 mb-6">
                <i class="far fa-heart"></i>
            </div>
            <h3 class="text-xl font-medium text-gray-900 mb-2">No favorites yet</h3>
            <p class="text-gray-500 mb-8 max-w-sm text-center">Click the heart icon on any listing to save it here and
                view it later.</p>
            <a href="/home?username=<%= encodeURIComponent(username) %>"
                class="bg-red-500 hover:bg-red-600 text-white font-medium py-3 px-8 rounded-full shadow-md transition">
                Start Exploring
            </a>
        </div>
    </main>

</body>

</html>