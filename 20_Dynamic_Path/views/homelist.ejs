<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb - All Listings</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-800">

    <!-- Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex-shrink-0 flex items-center">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-bold text-2xl tracking-tighter">airbnb</a>
                </div>
                <div class="hidden md:flex space-x-8">
                    <a href="/home?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Home</a>
                    <a href="/homelist?username=<%= encodeURIComponent(username) %>"
                        class="text-red-500 font-medium border-b-2 border-red-500 px-3 py-2">Listings</a>
                    <a href="/favourite?username=<%= encodeURIComponent(username) %>"
                        class="text-gray-600 hover:text-red-500 transition px-3 py-2 font-medium">Favorites</a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm font-medium text-gray-500">Hi, <%= username %></span>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-3xl font-bold text-gray-900">All Accommodations</h1>
            <div class="flex space-x-2">
                <button
                    class="bg-white border rounded-md px-4 py-2 text-sm font-medium hover:bg-gray-50">Filter</button>
                <button class="bg-white border rounded-md px-4 py-2 text-sm font-medium hover:bg-gray-50">Sort
                    by</button>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <% homes.forEach(home=> { %>
                <div class="bg-white rounded-lg shadow overflow-hidden hover:shadow-lg transition duration-300">
                    <div class="relative h-48">
                        <img src="<%= home.imageUrl %>" alt="<%= home.title %>" class="w-full h-full object-cover">
                    </div>
                    <div class="p-4">
                        <div class="flex justify-between items-start">
                            <h3 class="text-lg font-bold text-gray-900 truncate w-3/4">
                                <%= home.title %>
                            </h3>
                            <span class="flex items-center text-xs font-bold"><i
                                    class="fas fa-star text-yellow-500 mr-1"></i> 4.8</span>
                        </div>
                        <p class="text-gray-500 text-xs mb-3">
                            <%= home.location %>
                        </p>
                        <div class="mt-2 flex items-center justify-between">
                            <p class="font-semibold text-gray-900">$<%= home.price %> <span
                                        class="font-normal text-gray-500 text-xs">night</span></p>
                        </div>
                        <a href="/homedetails/<%= home.id %>?username=<%= encodeURIComponent(username) %>"
                            class="mt-3 block w-full text-center bg-red-500 hover:bg-red-600 text-white font-medium py-2 rounded transition">
                            View Details
                        </a>
                    </div>
                </div>
                <% }) %>
        </div>
    </main>
</body>

</html>